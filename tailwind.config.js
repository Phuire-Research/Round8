// Round8 Calculator - Tailwind CSS v4 Configuration
// HiFi Functional Design colors imported via CSS overrides (not config - classically faulty)

/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './demo/**/*.{html,js,ts}'
  ],
  darkMode: 'class',
};